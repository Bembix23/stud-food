import { View, Text } from 'react-native'
import React from 'react'
import { RouteProp } from '@react-navigation/native';

export default function recettesDetails(props: RecettesDetailsProps) {
  return (
    <View>
      <Text>Détail de la recette</Text>
    </View>
  )
}